# Claude Instructions for workout

## Git Workflow

After completing each story, always commit all changes and push to `main`:

```bash
git add .
git commit -m "story: STORY-XXX - <short description>"
git push
```

Never leave completed work uncommitted.
